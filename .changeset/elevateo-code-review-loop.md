---
type: Added
---
**`/gsd:code-review --loop` — dual-reviewer request→receive→address→re-review loop at phase boundary (roadmap #7)** — GSD had reviewer agents (`gsd-code-reviewer`, `gsd-ecc-code-reviewer`) but no explicit loop wiring that combined both reviewers, merged their findings, applied fixes, and iterated until clean. This extends `/gsd:code-review` with a `--loop` flag that activates the full request→receive→address→re-review loop (up to 3 rounds, configurable via `workflow.code_review_loop_max_rounds`). The new `code-review-loop.md` workflow spawns both reviewers in sequence, merges ECC findings into the adversarial REVIEW.md, applies fixes via `gsd-code-fixer`, and re-reviews until both reviewers approve or max rounds are reached. The loop is optional and non-blocking — phase completion is never prevented. Outcome is recorded in `{phase}-REVIEW-LOOP.md`. Mirrors the superpowers requesting-code-review / receiving-code-review discipline without reimplementing it. No new command file added; extends the existing `/gsd:code-review` command. INVENTORY.md drift reconciled: Commands (67→69), Workflows (88→91), References (61→62).
